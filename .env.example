# =============================================================================
# DNS Manager - Environment Configuration
# =============================================================================
# Only AUTH_SECRET is required. All other variables are optional.
# Copy this file to .env and configure as needed.
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - Must be set for the application to work
# -----------------------------------------------------------------------------

# NextAuth.js secret - Generate with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here"

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------

# Database type: sqlite (default), postgres, mysql, turso
# DATABASE_TYPE="sqlite"

# Database connection string or file path
# For SQLite: ./data/sqlite.db (default)
# For PostgreSQL: postgresql://user:password@host:5432/database
# For MySQL: mysql://user:password@host:3306/database
# For Turso: libsql://your-database.turso.io
# DATABASE_URL="./data/sqlite.db"

# Turso-specific settings (only needed for DATABASE_TYPE=turso)
# TURSO_DATABASE_URL="libsql://your-database.turso.io"
# TURSO_AUTH_TOKEN="your-turso-auth-token"

# -----------------------------------------------------------------------------
# DATABASE EXAMPLES
# -----------------------------------------------------------------------------

# --- SQLite (Default - Local Development) ---
# DATABASE_TYPE="sqlite"
# DATABASE_URL="./data/sqlite.db"

# --- PostgreSQL (Vercel Postgres) ---
# DATABASE_TYPE="postgres"
# DATABASE_URL="postgres://default:xxxxx@ep-xxx.us-east-1.aws.neon.tech/verceldb?sslmode=require"

# --- PostgreSQL (Neon) ---
# DATABASE_TYPE="postgres"
# DATABASE_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"

# --- PostgreSQL (Supabase) ---
# DATABASE_TYPE="postgres"
# DATABASE_URL="postgresql://postgres:password@db.xxx.supabase.co:5432/postgres"

# --- MySQL ---
# DATABASE_TYPE="mysql"
# DATABASE_URL="mysql://user:password@host:3306/database"

# --- Turso (Edge-compatible SQLite) ---
# DATABASE_TYPE="turso"
# TURSO_DATABASE_URL="libsql://your-database-xxx.turso.io"
# TURSO_AUTH_TOKEN="eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9..."

# -----------------------------------------------------------------------------
# OPTIONAL - Configure as needed
# -----------------------------------------------------------------------------

# GitHub OAuth (optional - for GitHub login)
# Create OAuth App at: https://github.com/settings/developers
# GITHUB_CLIENT_ID=""
# GITHUB_CLIENT_SECRET=""

# Dedicated encryption key for provider credentials (optional)
# If not set, AUTH_SECRET will be used for encryption
# Generate with: openssl rand -base64 32
# CREDENTIALS_ENCRYPTION_KEY=""

# Auth URL (required in production for proper redirects)
# AUTH_URL="https://your-domain.com"
